# SkyCoach AI - Local development extras
# ======================================
# Install on top of requirements.txt if you also want to run the
# Streamlit dashboard or train with the optional TF / PyTorch backends.
#
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# Optional accelerated training backends (CPU is fine, GPU is optional).
# Pin Python 3.11/3.12 - 3.14 has no wheels for these as of late 2025.
torch>=2.2.0
tensorflow>=2.16.0

# Streamlit dashboard / experimental UIs
streamlit>=1.31.0
streamlit-folium>=0.18.0
streamlit-extras>=0.3.0
streamlit-lottie>=0.0.5
streamlit-js-eval>=0.1.0
folium>=0.15.0

# Notebook execution for the submission notebook
nbclient>=0.10.0
nbformat>=5.10.0
ipykernel>=6.29.0
matplotlib>=3.8.0
seaborn>=0.13.0
