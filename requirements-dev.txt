# Local dev extras. Install on top of requirements.txt for ML training
# and submission-notebook execution.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# torch / tensorflow ship prebuilt wheels for Python 3.11 / 3.12; newer
# Python releases may not yet be supported.

-r requirements.txt

torch>=2.2.0
tensorflow>=2.16.0

nbclient>=0.10.0
nbformat>=5.10.0
ipykernel>=6.29.0
matplotlib>=3.8.0
seaborn>=0.13.0
pytest>=8.0.0
