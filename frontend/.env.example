# Backend API Configuration
# Local development (direct connection):
VITE_API_URL=http://localhost:8000
# Production: leave unset to use Netlify proxy (recommended)
# Or set to backend URL directly (requires CORS):
# VITE_API_URL=https://skycoach-ai.onrender.com

# App Configuration
VITE_APP_NAME=SkyCoach AI
