# Backend runtime dependencies. Cloud deploys install this file; the
# trained model ships as JSON so no deep-learning framework is needed
# at serving time. See requirements-dev.txt for local-only extras.

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

openai>=1.0.0
requests>=2.28.0
scikit-learn>=1.4.0
joblib>=1.4.0
numpy>=1.26.0

geopy>=2.4.0
pandas>=2.0.0
python-dotenv>=1.0.0
