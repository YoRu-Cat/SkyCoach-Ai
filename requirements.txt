# SkyCoach AI - Backend runtime dependencies
# ==========================================
# This file is what cloud deploys (Render, Railway, Fly, Docker) install.
# Only what the FastAPI server needs at runtime.
#
# For the full local toolkit (Streamlit app, optional TF/PyTorch training
# backends), install requirements-dev.txt on top of this.

# FastAPI server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Core AI / classifier runtime
openai>=1.0.0
requests>=2.28.0
scikit-learn>=1.4.0
joblib>=1.4.0
numpy>=1.26.0

# Geolocation lookups (used by services/geolocation.py)
geopy>=2.4.0

# Data utilities used by some services
pandas>=2.0.0

# Environment & config
python-dotenv>=1.0.0
